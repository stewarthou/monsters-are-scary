{
  "name": "monsters-are-scary",
  "version": "1.0.0",
  "private": true,
  "description": "Monsters Are Scary is a text adventure game requiring the player to crawl through a dungeon fighting monsters and collecting gold.",
  "scripts": {
    "test": "NODE_ENV=test standard && mocha",
    "lint": "standard",
    "test-only": "NODE_ENV=test mocha"
  },
  "author": "Yunzhi Hou",
  "license": "ISC",
  "engines": {
    "node": ">=8.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "node-mocks-http": "^1.7.0",
    "standard": "^11.0.1"
  },
  "dependencies": {
    "config": "^1.30.0",
    "debug": "^3.1.0"
  }
}
